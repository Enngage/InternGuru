@model UI.Builders.Auth.Views.AuthEditCompanyView

@{
    Model.Metadata.BasicMetadata.Title = $"Editovat | {(Model.CompanyForm.IsExistingCompany ? Model.CompanyForm.CompanyName : string.Empty)}";
    Layout = Model.AuthMaster.AuthMasterLayout;
}

@Html.Partial("Partials/AddEditCompany", Model.CompanyForm)